<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TDMeteo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/background</item>
        <item name="colorOnPrimary">@color/list</item>
        <item name="colorAccent">@color/yellow</item>
        <item name="buttonStyle">@style/FloatingActionButtonStyle</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TDMeteo" parent="Base.Theme.TDMeteo" />

    <style name="FloatingActionButtonStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="android:backgroundTint">@color/yellow</item>
        <item name="android:tint">@color/black</item>
    </style>

</resources>